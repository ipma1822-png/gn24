(()=>{'use strict';const cfg=window.GN24_SUPABASE||{},base=String(cfg.url||'').replace(/\/$/,''),key=String(cfg.anonKey||''),$=id=>document.getElementById(id),storageKey='gn24-final-admin-session';let token='',submissionId='',current=null;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function read(){try{return JSON.parse(localStorage.getItem(storageKey)||'{}')}catch{return{}}}function write(s){if(!s?.access_token)return;const x={access_token:s.access_token,refresh_token:s.refresh_token||'',expires_at:Number(s.expires_at||0)};if(!x.expires_at&&s.expires_in)x.expires_at=Math.floor(Date.now()/1000)+Number(s.expires_in);localStorage.setItem(storageKey,JSON.stringify(x));return x}function jwtExp(t){try{const p=t.split('.')[1].replace(/-/g,'+').replace(/_/g,'/'),j=JSON.parse(decodeURIComponent(atob(p).split('').map(c=>'%'+('00'+c.charCodeAt(0).toString(16)).slice(-2)).join('')));return Number(j.exp||0)}catch{return 0}}function consumeHash(){const p=new URLSearchParams(location.hash.slice(1));if(!p.get('access_token'))return null;const s=write({access_token:p.get('access_token'),refresh_token:p.get('refresh_token')||'',expires_in:Number(p.get('expires_in')||0),expires_at:Number(p.get('expires_at')||0)||jwtExp(p.get('access_token'))});history.replaceState(null,'',location.pathname+location.search);return s}async function refresh(rt){if(!rt)return null;const r=await fetch(base+'/auth/v1/token?grant_type=refresh_token',{method:'POST',headers:{apikey:key,'Content-Type':'application/json'},body:JSON.stringify({refresh_token:rt})});if(!r.ok)return null;return write(await r.json())}async function ensure(){let s=consumeHash()||read();if(!s?.access_token)return null;const exp=Number(s.expires_at||jwtExp(s.access_token)||0),now=Math.floor(Date.now()/1000);if(exp&&exp<=now+120&&s.refresh_token)s=await refresh(s.refresh_token)||s;if(exp&&exp<=now&&!s.refresh_token)return null;token=s.access_token;return s}async function req(path,opt={}){const r=await fetch(base+path,{...opt,headers:{apikey:key,Authorization:`Bearer ${token||key}`,...(opt.body?{'Content-Type':'application/json'}:{}),...(opt.headers||{})}});if(!r.ok)throw new Error(await r.text()||`HTTP ${r.status}`);return r.status===204?null:r.json()}async function rpc(name,body={}){return req('/rest/v1/rpc/'+name,{method:'POST',body:JSON.stringify(body)})}async function isAdmin(){if(!token)return false;try{return await rpc('is_gn24_admin')===true}catch{return false}}async function sendLink(){const email=$('email').value.trim();if(!email)return msg('최종관리자 이메일을 입력해 주세요.');const redirect=new URL('/pages/admin-final-publish/',location.origin);redirect.searchParams.set('id',submissionId);const r=await fetch(base+'/auth/v1/otp',{method:'POST',headers:{apikey:key,'Content-Type':'application/json'},body:JSON.stringify({email,create_user:true,options:{emailRedirectTo:redirect.toString()}})});msg(r.ok?'이메일로 최종관리자 로그인 링크를 보냈습니다. 링크를 누르면 이 기사 승인 화면으로 돌아옵니다.':'로그인 링크를 보내지 못했습니다.')}function msg(t){$('msg').hidden=false;$('msg').textContent=t}function renderMedia(urls){const a=Array.isArray(urls)?urls.filter(Boolean):[];$('media').innerHTML=a.length?a.map((u,i)=>`<img src="${esc(u)}" alt="기사 사진 ${i+1}">`).join(''):'<span class="meta">첨부 사진 없음</span>'}async function load(){submissionId=new URLSearchParams(location.search).get('id')||'';if(!submissionId){$('guard').textContent='기사 ID가 없습니다.';return}const rows=await req('/rest/v1/gn24_reporter_submissions?select=id,status,reporter_id,editor_title,editor_subtitle,editor_summary,editor_body,editor_image_caption,editor_category,editor_keywords,editor_edited_at,media_urls,linked_article_id&id=eq.'+encodeURIComponent(submissionId));current=rows?.[0]||null;if(!current){$('guard').textContent='기사를 찾지 못했습니다.';return}$('guard').hidden=true;$('article').hidden=false;$('title').textContent=current.editor_title||'제목 없음';$('category').textContent=current.editor_category||'종합';$('summary').textContent=current.editor_summary||'';$('body').textContent=current.editor_body||'';$('caption').textContent=current.editor_image_caption||'';renderMedia(current.media_urls);if(current.status==='published'&&current.linked_article_id){$('publish').hidden=true;$('publishedLink').href='/pages/article/?id='+encodeURIComponent(current.linked_article_id);$('publishedLink').hidden=false}}async function publish(){if(!current||current.status==='published')return;if(!current.editor_title?.trim()||!current.editor_body?.trim())return alert('제목과 본문이 저장되어 있어야 합니다.');if(!confirm('이 기사를 Global News24에 최종 승인하여 공개 발행하시겠습니까?'))return;const b=$('publish');b.disabled=true;try{const out=await rpc('gn24_publish_submission',{p_submission_id:submissionId}),row=Array.isArray(out)?out[0]:out;if(!row?.article_id)throw new Error('기사 ID 확인 실패');alert('최종 승인·발행이 완료되었습니다.');location.href='/pages/article/?id='+encodeURIComponent(row.article_id)}catch(e){console.error(e);alert('발행 실패: '+e.message)}finally{b.disabled=false}}document.addEventListener('DOMContentLoaded',async()=>{submissionId=new URLSearchParams(location.search).get('id')||'';$('send').addEventListener('click',sendLink);$('publish').addEventListener('click',publish);$('logout').addEventListener('click',()=>{localStorage.removeItem(storageKey);location.reload()});await ensure();if(await isAdmin()){$('login').hidden=true;$('admin').hidden=false;await load()}else{$('login').hidden=false;$('admin').hidden=true}});})();