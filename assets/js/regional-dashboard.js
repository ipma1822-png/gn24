(()=>{'use strict';const cfg=window.GN24_SUPABASE||{},base=String(cfg.url||'').replace(/\/$/,''),key=String(cfg.anonKey||''),$=id=>document.getElementById(id);let token='';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function session(){try{token=JSON.parse(localStorage.getItem('gn24-editorial-session')||'{}').access_token||''}catch{token=''}}async function rpc(name){const r=await fetch(base+'/rest/v1/rpc/'+name,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${token||key}`,'Content-Type':'application/json'},body:'{}'});if(!r.ok)throw new Error(await r.text()||`HTTP ${r.status}`);return r.json()}function render(d){const isHQ=d.scope_type==='regional_hq',label=isHQ?`${d.hq_code||''} 시·도본부`:`${d.branch_code||''} 시·군·구 지부`;$('title').textContent=label+' 운영 대시보드';$('scope').textContent=`${d.position_name||''} · 담당 범위 안의 운영정보만 표시됩니다.`;$('reporterCount').textContent=d.reporter_count||0;const c=d.contribution_summary||{};$('paid').textContent=c.paid||0;$('unpaid').textContent=c.unpaid||0;$('deferred').textContent=c.deferred||0;$('exempt').textContent=c.exempt||0;if(!isHQ)$('branchesPanel').hidden=true;else $('branches').innerHTML=(d.branches||[]).map(b=>`<span class="tag">${esc(b.name)} · ${esc(b.status)}</span> `).join('')||'현재 등록된 지부가 없습니다.';$('reporters').innerHTML=(d.reporters||[]).map(r=>`<tr><td><b>${esc(r.name)}</b></td><td>${esc(r.reporter_number||'-')}</td><td>${esc(r.reporter_rank||'기자')}</td><td>${esc(r.reporter_type||'')}</td><td>${esc(r.organization_position||'-')}</td><td>${esc(r.regional_branch_code||'-')}</td></tr>`).join('')||'<tr><td colspan="6">현재 담당지역 소속 기자가 없습니다.</td></tr>';$('guard').hidden=true;$('content').hidden=false}document.addEventListener('DOMContentLoaded',async()=>{session();if(!token){$('guard').textContent='기자 로그인이 필요합니다.';return}try{const d=await rpc('gn24_my_regional_dashboard');render(d)}catch(e){console.error(e);$('guard').innerHTML='현재 계정에는 시·도본부장 또는 시·군·구 지부장 권한이 없습니다. 본사에서 위촉 및 담당지역 연결을 확인해 주세요.'}});})();