(()=>{
  'use strict';
  const $=(s,p=document)=>p.querySelector(s);
  const state={articles:[],selectedId:null,dirty:false,imageFile:null,imageObjectUrl:null};
  const DEFAULT_IMAGE='/assets/images/news/gn24-default-news.svg';
  const els={
    list:$('#articleList'),count:$('#articleCount'),listCount:$('#listCount'),dirty:$('#dirtyState'),search:$('#searchInput'),form:$('#articleForm'),
    id:$('#fId'),date:$('#fDate'),title:$('#fTitle'),subtitle:$('#fSubtitle'),category:$('#fCategory'),author:$('#fAuthor'),summary:$('#fSummary'),image:$('#fImage'),caption:$('#fImageCaption'),content:$('#fContent'),sourceName:$('#fSourceName'),sourceUrl:$('#fSourceUrl'),tags:$('#fTags'),featured:$('#fFeatured'),pinned:$('#fPinned'),visualStyle:$('#fVisualStyle'),preview:$('#imagePreview'),imageInput:$('#imageInput'),imageFilename:$('#imageFilename'),downloadImage:$('#downloadImageBtn'),saveMessage:$('#saveMessage')
  };
  const clean=s=>String(s??'').trim();
  const clone=x=>JSON.parse(JSON.stringify(x));
  const ymd=d=>String(d||'').replaceAll('-','');
  const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
  const makeId=(date=today())=>`gn24-${ymd(date)}-${String(Date.now()).slice(-6)}`;
  const sortArticles=a=>[...a].sort((x,y)=>String(y.date||'').localeCompare(String(x.date||''))||String(y.id||'').localeCompare(String(x.id||'')));
  function setDirty(v=true){state.dirty=v;els.dirty.textContent=v?'저장본 변경됨':'원본 상태';els.dirty.style.color=v?'#b42318':'';}
  function current(){return state.articles.find(a=>a.id===state.selectedId)||null}
  function imageBg(src){els.preview.style.backgroundImage=`url("${src||DEFAULT_IMAGE}"),url("${DEFAULT_IMAGE}")`;els.preview.textContent=''}
  function resetImageFile(){if(state.imageObjectUrl)URL.revokeObjectURL(state.imageObjectUrl);state.imageObjectUrl=null;state.imageFile=null;els.imageInput.value='';els.downloadImage.disabled=true}
  function select(id){state.selectedId=id;resetImageFile();const a=current();if(!a)return;els.id.value=a.id||'';els.date.value=a.date||today();els.title.value=a.title||'';els.subtitle.value=a.subtitle||'';els.category.value=a.category||'국내소식';if(![...els.category.options].some(o=>o.value===(a.category||''))){const o=document.createElement('option');o.value=o.textContent=a.category||'뉴스';els.category.append(o);els.category.value=o.value}els.author.value=a.author||'Global News24 편집부';els.summary.value=a.summary||'';els.image.value=a.image||'';els.caption.value=a.imageCaption||'';els.content.value=(Array.isArray(a.content)?a.content:(Array.isArray(a.body)?a.body:[])).join('\n\n');els.sourceName.value=a.sourceName||'';els.sourceUrl.value=a.sourceUrl||'';els.tags.value=(a.tags||[]).join(', ');els.featured.checked=!!a.featured;els.pinned.checked=!!a.pinned;els.visualStyle.value=a.visualStyle||'normal';els.imageFilename.value=(a.image||'').split('/').pop()||`${ymd(a.date)}-news.jpg`;imageBg(a.image);$('#editorTitle').textContent=`기사 편집 · ${a.category||'뉴스'}`;els.saveMessage.textContent='편집 후 저장을 눌러주세요.';renderList()}
  function formData(){const paragraphs=els.content.value.split(/\n\s*\n/).map(clean).filter(Boolean);return {id:clean(els.id.value)||makeId(els.date.value),title:clean(els.title.value),subtitle:clean(els.subtitle.value),date:els.date.value||today(),category:els.category.value||'뉴스',summary:clean(els.summary.value),image:clean(els.image.value)||DEFAULT_IMAGE,author:clean(els.author.value)||'Global News24 편집부',sourceName:clean(els.sourceName.value)||'Global News24',sourceUrl:clean(els.sourceUrl.value),tags:els.tags.value.split(',').map(clean).filter(Boolean),content:paragraphs,featured:els.featured.checked,visualStyle:els.visualStyle.value||'normal',pinned:els.pinned.checked,relatedOrgs:current()?.relatedOrgs||[],...(clean(els.caption.value)?{imageCaption:clean(els.caption.value)}:{})}}
  function renderList(){const q=clean(els.search.value).toLowerCase();let data=sortArticles(state.articles);if(q)data=data.filter(a=>JSON.stringify(a).toLowerCase().includes(q));els.count.textContent=state.articles.length;els.listCount.textContent=`표시 ${data.length}건`;els.list.innerHTML='';if(!data.length){els.list.innerHTML='<div class="empty">검색 결과가 없습니다.</div>';return}for(const a of data){const b=document.createElement('button');b.type='button';b.className='article-item'+(a.id===state.selectedId?' active':'');b.innerHTML=`<b></b><small><span></span><span></span></small>`;b.querySelector('b').textContent=a.title||'(제목 없음)';b.querySelectorAll('small span')[0].textContent=a.date||'';b.querySelectorAll('small span')[1].textContent=a.category||'뉴스';b.onclick=()=>{if(state.dirty&&state.selectedId&&els.saveMessage.textContent.includes('저장하지')){if(!confirm('현재 편집 내용을 저장하지 않고 다른 기사로 이동할까요?'))return}select(a.id)};els.list.append(b)}}
  async function loadSite(){const r=await fetch('/data/news.json?admin='+Date.now(),{cache:'no-store'});if(!r.ok)throw new Error('news.json을 불러오지 못했습니다.');const data=await r.json();if(!Array.isArray(data))throw new Error('news.json 형식이 올바르지 않습니다.');state.articles=data;state.selectedId=sortArticles(data)[0]?.id||null;setDirty(false);renderList();if(state.selectedId)select(state.selectedId)}
  function newArticle(){const d=today(),id=makeId(d);state.articles.unshift({id,title:'',subtitle:'',date:d,category:'국내소식',summary:'',image:DEFAULT_IMAGE,author:'Global News24 편집부',sourceName:'Global News24',sourceUrl:'',tags:[],content:[],featured:false,pinned:false,visualStyle:'normal',relatedOrgs:[]});state.selectedId=id;setDirty(true);select(id);els.title.focus();els.saveMessage.textContent='새 기사를 작성한 뒤 저장하세요.'}
  function saveCurrent(e){e?.preventDefault();const a=formData();if(!a.title){alert('기사 제목을 입력해 주세요.');els.title.focus();return}const oldId=state.selectedId;const idx=state.articles.findIndex(x=>x.id===oldId);if(idx<0)state.articles.push(a);else state.articles[idx]=a;state.selectedId=a.id;setDirty(true);renderList();els.saveMessage.textContent='현재 기사가 편집본에 저장되었습니다.'}
  function deleteCurrent(){const a=current();if(!a)return;if(!confirm(`이 기사를 편집본에서 삭제할까요?\n\n${a.title||a.id}`))return;state.articles=state.articles.filter(x=>x.id!==a.id);state.selectedId=sortArticles(state.articles)[0]?.id||null;setDirty(true);renderList();if(state.selectedId)select(state.selectedId);else newArticle()}
  function duplicate(){const a=current();if(!a)return;const c=clone(a);c.id=makeId(c.date||today());c.title=`${c.title} (복사본)`;c.pinned=false;state.articles.unshift(c);state.selectedId=c.id;setDirty(true);select(c.id)}
  function downloadBlob(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.append(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},1000)}
  function exportJSON(){if(state.selectedId)saveCurrent();const out=sortArticles(state.articles);downloadBlob(new Blob([JSON.stringify(out,null,2)+'\n'],{type:'application/json;charset=utf-8'}),'news.json');els.saveMessage.textContent='news.json을 내려받았습니다. GitHub data/news.json에 교체 업로드하세요.'}
  async function importJSON(file){const txt=await file.text();const data=JSON.parse(txt);if(!Array.isArray(data))throw new Error('기사 배열 형식이 아닙니다.');state.articles=data;state.selectedId=sortArticles(data)[0]?.id||null;setDirty(true);renderList();if(state.selectedId)select(state.selectedId)}
  function chooseImage(file){resetImageFile();if(!file)return;state.imageFile=file;state.imageObjectUrl=URL.createObjectURL(file);imageBg(state.imageObjectUrl);const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'')||'jpg';const base=`${ymd(els.date.value||today())}-news-${String(Date.now()).slice(-4)}.${ext}`;els.imageFilename.value=base;els.image.value=`/assets/images/news/${base}`;els.downloadImage.disabled=false;els.caption.focus();els.saveMessage.textContent='새 이미지가 선택되었습니다. 기사 저장 후 이미지도 내려받으세요.'}
  function downloadImage(){if(!state.imageFile)return;const name=clean(els.imageFilename.value)||state.imageFile.name;downloadBlob(state.imageFile,name)}
  $('#newBtn').onclick=newArticle;$('#exportBtn').onclick=exportJSON;$('#deleteBtn').onclick=deleteCurrent;$('#duplicateBtn').onclick=duplicate;$('#restoreBtn').onclick=()=>{if(confirm('현재 편집본을 버리고 사이트의 news.json을 다시 불러올까요?'))loadSite().catch(e=>alert(e.message))};els.search.oninput=renderList;els.form.onsubmit=saveCurrent;els.importInput=$('#importInput');els.importInput.onchange=async e=>{try{if(e.target.files[0])await importJSON(e.target.files[0])}catch(err){alert('불러오기 실패: '+err.message)}finally{e.target.value=''}};els.imageInput.onchange=e=>chooseImage(e.target.files[0]);els.downloadImage.onclick=downloadImage;els.image.oninput=()=>{if(!state.imageFile)imageBg(clean(els.image.value));els.saveMessage.textContent='저장하지 않은 변경 내용이 있습니다.'};els.form.addEventListener('input',e=>{if(!['fImage'].includes(e.target.id))els.saveMessage.textContent='저장하지 않은 변경 내용이 있습니다.'});window.addEventListener('beforeunload',e=>{if(state.dirty){e.preventDefault();e.returnValue=''}});
  loadSite().catch(err=>{els.list.innerHTML=`<div class="empty">${err.message}<br>상단의 news.json 불러오기를 이용해 주세요.</div>`;newArticle()});
})();
